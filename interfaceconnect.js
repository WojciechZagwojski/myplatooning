class connectPlatooning {

    Collection<indexPosition, genesisRoot>

    connectPlatooning()

    String generateChatRoot()

    int getLastIndexPosition()

    String proofAddressesForNewVehicle(address)

    Response sendAccess(address, chatRoot, indexPosition)

    Response disconnectPlatooning(indexPosition)

    Response attachPlatoon(notify, address)

    Response detachPlatoon(indexPosition)


}

class streamData {

    String generateNewRoot(nextRoot, indexPosition, mamState)

    //void saveRoot(nextRoot, indexPosition)

    String getRoot(indePosition)

    setRoot(indexPosition, nextRoot)

    Response checkValidPay(iota, indexPosition, address)

    Response proofNextRoot(nextRoot)

    Objekt chatStreamData(data, indexPosition, mamState)

    Objekts/Strings decodeData(root)
}

class generateData {


}